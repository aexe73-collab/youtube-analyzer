{
  "name": "youtube-analyzer",
  "version": "1.0.0",
  "scripts": {
    "build": "cp index.html dist/ || mkdir dist && cp index.html dist/"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.20.0"
  }
}
